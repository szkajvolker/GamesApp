const MoreDetailsModal = ({ game, onClose }) => {
  if (!game) return null;
  return (
    <div>
      <div>
        <button></button>
      </div>
    </div>
  );
};

export default MoreDetailsModal;
